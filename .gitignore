# Environment
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / frontend
node_modules/
dist/
.vite/
coverage/

# Playwright CLI scratch artifacts (browser snapshots, console/network logs)
.playwright-cli/

# Supabase
supabase/.branches/
supabase/.temp/

# OS
.DS_Store
Thumbs.db

# IDEs
.vscode/
.idea/
*.swp

# Logs
*.log
npm-debug.log*

# Eval harness — per-run JSON in evals/runs/ IS committed (no merge
# conflicts across PRs). The derived SQLite is not — rebuild it locally
# with `python eval.py --report`.
evals/.cache/
evals/results.db

# Personal project memory (cowork OS) — local only
memory.md
